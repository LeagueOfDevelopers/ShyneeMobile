import {StyleSheet} from 'react-native';

import {fonts, colors} from '../../../constants/styles';

export default StyleSheet.create({
  icon: {
    marginRight: fonts.size.large,
    color: colors.grey
  }
});